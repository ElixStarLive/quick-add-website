# Server Configuration
PORT=3000
NODE_ENV=production

# Your live site URL (no trailing slash)
SITE_URL=https://quickpostads.co.uk

# Stripe — https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here
# Enable Clearpay in Stripe Dashboard → Settings → Payment methods (UK, £1–£1,200)

# OpenAI — optional for AI-enhanced job classification (https://platform.openai.com/api-keys)
OPENAI_API_KEY=your_openai_key_here
# Optional: gpt-4o (default) or gpt-4o-mini (cheaper)
# OPENAI_MODEL=gpt-4o

# Security — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SECRET=generate_a_long_random_secret_here
SESSION_SECRET=generate_another_long_random_secret_here

# Database — use persistent path on VPS (e.g. /var/www/quickpostads/quickpost.db)
DATABASE_PATH=./quickpost.db
